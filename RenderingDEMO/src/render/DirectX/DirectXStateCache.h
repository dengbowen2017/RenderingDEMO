#pragma once

namespace RenderingDEMO
{
	// Track the current rendering state
	// containing all the information needed, vertex buffer, input layout, shader...
	// when clear the state, only reset the pointer, not release the resource
	// we don't change anything in this state. We just set them

	//class DirectXStateCache
	//{
	//public:
	//	DirectXStateCache();
	//	~DirectXStateCache();

	//private:

	//};
}